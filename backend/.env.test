# Test Environment Configuration
NODE_ENV=test
PORT=5001

# Test Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=muktamar_agm_test
DB_USER=postgres
DB_PASSWORD=postgres

# JWT
JWT_SECRET=test_secret_key_for_testing_purposes_min_32_chars
JWT_EXPIRY=24h

# Email
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@ethereal.email
SMTP_PASS=test_password
SMTP_FROM=noreply@muktamar.local

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS
CORS_ORIGIN=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Logging
LOG_LEVEL=error
